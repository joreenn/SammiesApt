import React from "react";
import ReactDOM from "react-dom/client";
import TenantManagement from "./TenantManagement"; // adjust import if needed
import '../../css/app.css';


ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <TenantManagement />
  </React.StrictMode>
);
